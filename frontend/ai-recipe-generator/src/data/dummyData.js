// Pure AI Recipe Hub - No hardcoded static recipes.
// All recipes are dynamically generated by Gemini AI or created by users in MongoDB.

export const dummyUser = {
  id: 1,
  name: 'User',
  email: 'user@example.com',
  created_at: new Date().toISOString(),
};

export const dummyPreferences = {
  user_id: 1,
  dietary_restrictions: [],
  preferred_cuisines: [],
  default_servings: 4,
  measurement_unit: 'metric',
};

// Pure Dynamic Array - No static hardcoded recipes
export const dummyRecipes = [];

export const dummyPantryItems = [];

export const dummyMealPlans = [];

export const dummyShoppingList = [];

// Helper functions
export const getExpiringItems = () => [];

export const getRecentRecipes = (limit = 5) => [];

export const getUpcomingMeals = (limit = 5) => [];

export const getRecipeById = (id) => null;

export const simulateApiCall = (data, delay = 100) => {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve({ data: { data } });
    }, delay);
  });
};
